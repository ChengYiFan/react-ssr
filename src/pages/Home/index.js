import React from 'react';

const home = () => {
  return (
    <div>
      <div>React Component, hello, haha!</div>
      <button onClick={() => {alert('click')}}>click</button>
    </div>
  );
};

export default home;
